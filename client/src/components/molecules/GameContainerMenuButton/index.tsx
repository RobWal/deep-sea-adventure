import './GameContainerMenuButton.css';

type GameContainerMenuButton = {
    className: string,
};

const GameContainerMenuButton = ({className}: GameContainerMenuButton) => {
    return (
        <div className={className}>

        </div>
    )
};

export default GameContainerMenuButton;